<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% if (!user) { %>
    <form action= "/login" method= "POST" style="margin:0px;">
      <input id="user" type="text" name="user" placeholder="Username" style="width: 200px">
      <input type="submit" value="Login">
    </form>
  <% } else { %>
    <p>Logged in as <%= users[user_id].email %></p>
    <form action= "/logout" method= "POST">
      <input type="submit" value="Logout">
    </form>
  <% } %>
</body>
</html>