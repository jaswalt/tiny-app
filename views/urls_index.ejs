<% include ./partials/_header %>

<body>
    <h2>URLs</h2>
      <ul>
        <% for(url in urls) { %>
          <li><%= url %> : <%= urls[url].longURL %></li>
        <% }; %>
      </ul>

      <p><a href=>Shorten New Link</a></p>

</body>